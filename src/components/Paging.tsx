import React from 'react'
import Pagination from 'react-bootstrap/Pagination';

const Pag = ( numPages: Number) => {
    debugger
   // const pageNumbers = [...Array(numPages + 1).keys()].slice(1)
//   const pageNumbers = [Array.from(Array(numPages + 1)).keys()].slice(1)
    
    return (
        <nav>
           
        </nav>
    )
}

export default Pag